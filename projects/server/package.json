{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "Should probably write something up about how to start the server.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
